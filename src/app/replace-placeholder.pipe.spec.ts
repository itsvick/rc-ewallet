import { ReplacePlaceholderPipe } from './replace-placeholder.pipe';

describe('ReplacePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
